{
  "description": "Build emits a warning above a baseline of 0 — the ratchet must FAIL (exit 1). The build still COMPILED, so the short-circuit does not apply: format and tests run and pass. Only a failed compile makes downstream stages impossible; a ratchet failure does not, so hiding them behind a SKIP would tell you less.",
  "mode": "--fast",
  "exit": 1,
  "verdict": "FAIL",
  "stages": [
    { "id": "1a", "status": "FAIL", "detailContains": "0" },
    { "id": "1b", "status": "PASS" },
    { "id": "2",  "status": "PASS" }
  ]
}
