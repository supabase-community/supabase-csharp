{
  "description": "A failing E2E test in full mode with the stack reachable. Build and format are green; the unfiltered run (unit + contract + E2E together) includes the failing acceptance test, so id 2 — not a separate E2E stage — goes FAIL. Coverage is still measured from that same run despite the test failure. Guards the rule that a red acceptance test blocks the merge exactly like a red unit test.",
  "mode": "",
  "needsStack": true,
  "exit": 1,
  "verdict": "FAIL",
  "stages": [
    { "id": "1a", "status": "PASS" },
    { "id": "1b", "status": "PASS" },
    { "id": "2",  "status": "FAIL", "detailContains": "Failed" },
    { "id": "2b", "status": "PASS" }
  ]
}
