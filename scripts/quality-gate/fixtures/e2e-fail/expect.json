{
  "description": "A failing E2E test in standard (full) mode. Build, format and the inner loop are green and the stack is reachable, so E2E actually runs — and because E2E is blocking, its failure must FAIL the gate (exit 1). Guards the rule that a red acceptance test blocks the merge exactly like a red unit test.",
  "mode": "",
  "needsStack": true,
  "exit": 1,
  "verdict": "FAIL",
  "stages": [
    { "id": "1a", "status": "PASS" },
    { "id": "1b", "status": "PASS" },
    { "id": "2",  "status": "PASS" },
    { "id": "7",  "status": "FAIL", "detailContains": "Failed" }
  ]
}
